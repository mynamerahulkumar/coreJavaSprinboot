package com.corejava.corejavasprinboot.threads;

/**
 * 2. What is the difference between start() and run() method in Java Threads?
 * start(): Starts a new thread and invokes the run() method asynchronously.
 * run(): Runs in the same thread that called it, without creating a new thread.
 */
public class ThreadStartAndRun2 {
}
